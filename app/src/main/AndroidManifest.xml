<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permiso de Internet CLAVE para la API -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/sound_logo"
        android:roundIcon="@drawable/sound_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReproductorMusic">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label=""
            android:theme="@style/Theme.ReproductorMusic">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HomeActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false">
        </activity>

        <!-- 🚨 ACTIVIDAD DE BÚSQUEDA AÑADIDA 🚨 -->
        <activity
            android:name=".SearchActivity"
            android:exported="false">
        </activity>

    </application>
</manifest>
